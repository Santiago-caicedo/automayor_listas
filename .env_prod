# ============================================
# .env PRODUCCIÓN - AUTOMAYOR LISTAS
# ============================================
# Copiar este contenido al .env del servidor

# --- Django ---
SECRET_KEY='CAMBIAR-POR-UNA-CLAVE-SEGURA-GENERADA'
DEBUG=False

# --- Dominios y Seguridad ---
ALLOWED_HOSTS=automayor.vadomconsultas.com,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://automayor.vadomconsultas.com
MI_DOMINIO=https://automayor.vadomconsultas.com

# --- Base de Datos PostgreSQL (RDS) ---
DB_NAME='automayorlistas_db'
DB_USER='usr.automayorlistas_db'
DB_PASSWORD='Awt83vNJidSpz47U'
DB_HOST='vadom-consulting-db.cotou8cmuagz.us-east-1.rds.amazonaws.com'
DB_PORT='5432'

# --- API ConsultaListasPeps ---
API_TOKEN='8495-4545-4487'
API_BASE_URL='https://www.consultalistaspeps.com/ClientArea/BLS_WS_BLS/ConsultaListasPeps.svc/rest/'

# --- AWS S3 ---
# No se necesitan AWS_ACCESS_KEY_ID ni AWS_SECRET_ACCESS_KEY
# El EC2 usa IAM Role para acceder a S3 automáticamente
S3_CLIENT_PREFIX=automayor

# --- Email SMTP ---
EMAIL_HOST='COMPLETAR'
EMAIL_PORT=587
EMAIL_HOST_USER='COMPLETAR'
EMAIL_HOST_PASSWORD='COMPLETAR'
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
ADMIN_EMAIL='vadomdata@gmail.com'
